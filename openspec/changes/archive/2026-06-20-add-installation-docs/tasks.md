# Tasks: add-installation-docs

- [x] Add an "Installation" section to `README.md` (`go install`, release binaries, from-source/`mise`+`task`) and a `flow.md` row in the docs table.
- [x] Update `docs/getting-started.md` prerequisites (mise + Go 1.26.4) and the "Install or build" step; add `mise`/`task ci` note to `CONTRIBUTING.md`.
- [x] Wire `flow.md` into `docs/README.md` (reading path + index); verify all links resolve and `go build`/`go test` (116) stay green.
